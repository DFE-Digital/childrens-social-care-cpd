version: '3.4'

services:
   cypress:
      image: "cypress/included:3.2.0"
      entrypoint: cypress

      environment:
       CYPRESS_baseUrl="https://localhost:44337/"
       specPattern=
      working_dir: /e2e
      
   